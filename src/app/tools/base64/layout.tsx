import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Base64转换 - 图片与Base64互转',
  description: '在线Base64转换工具，支持图片与Base64编码互转，方便嵌入代码或传输，一键复制结果。',
};

export { default } from './page';

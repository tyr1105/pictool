import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: '图片裁剪 - 可视化裁剪图片',
  description: '在线图片裁剪工具，可视化裁剪图片区域，支持自由裁剪和多种预设比例（1:1、4:3、16:9等）。',
};

export { default } from './page';
